import { NextResponse } from "next/server";
import { createSupabaseServerClient } from "@/lib/supabase-server";
export const dynamic="force-dynamic";export const revalidate=0;
type Body={playerToken?:unknown;matchToken?:unknown;taps?:unknown};
const UUID_RE=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
function json(body:Record<string,unknown>,status=200){return NextResponse.json(body,{status,headers:{"Cache-Control":"no-store, no-cache, must-revalidate, max-age=0"}});}
export async function POST(request:Request,context:{params:Promise<{slug:string}>}){try{const{slug}=await context.params;const body=(await request.json()) as Body;const p=typeof body.playerToken==="string"?body.playerToken.trim():"";const m=typeof body.matchToken==="string"?body.matchToken.trim():"";const taps=typeof body.taps==="number"&&Number.isInteger(body.taps)?body.taps:0;if(!UUID_RE.test(p))return json({error:"Player session expired."},401);if(!UUID_RE.test(m))return json({error:"Match not found."},400);if(taps<1||taps>10)return json({error:"Invalid tap batch."},400);const s=await createSupabaseServerClient();const{data:e}=await s.from("events").select("id").eq("event_slug",slug).maybeSingle();if(!e)return json({error:"Event not found."},404);const{data,error}=await s.rpc("submit_glitter_tap_batch_v1",{p_event_id:String(e.id),p_player_token:p,p_match_token:m,p_taps:taps});if(error)return json({error:error.message},error.message.toLowerCase().includes("not active")?409:400);const result=Array.isArray(data)?data[0]:data;return result?json({result}):json({error:"Unable to record taps."},500);}catch(error){console.error("Tap batch failed:",error);return json({error:"Unable to record taps."},500);}}
